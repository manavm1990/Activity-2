// Booleans: either true or false
const isAdult = true;
const canDrink = false;

// Null: empty value
const nothing = null;

// Undefined: no value assigned
let name;

name = "Makr";

// Reference Error - 'not defined' (not the same as 'undefined')
console.log(whatever);
